
# 1) Proxy TCP pour PostgreSQL (stream)
stream {
    upstream pgsql {
        server 127.0.0.1:5433;
    }

    server {
        listen 5433 ssl;                      
        proxy_pass pgsql;                     

        ssl_certificate     /etc/letsencrypt/live/like-it-bdd-pre-prod.coak.fr/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/like-it-bdd-pre-prod.coak.fr/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;
    }
}

http {
    # 2) Catch-all HTTP → HTTPS
    server {
        listen 80;
        server_name like-it-pre-prod.coak.fr
                    like-it-api-pre-prod.coak.fr
                    like-it-bdd-pre-prod.coak.fr;
        return 301 https://$host$request_uri;
    }

    # 3) Frontend
    server {
        listen 443 ssl;
        server_name like-it.coak.fr;

        ssl_certificate     /etc/letsencrypt/live/like-it-pre-prod.coak.fr/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/like-it-pre-prod.coak.fr/privkey.pem;
        include             /etc/letsencrypt/options-ssl-nginx.conf;
        ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

        location / {
            proxy_pass         http://127.0.0.1:3001;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }

    # 4) Backend API
    server {
        listen 443 ssl;
        server_name like-it-api.coak.fr;

        ssl_certificate     /etc/letsencrypt/live/like-it-api-pre-prod.coak.fr/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/like-it-api-pre-prod.coak.fr/privkey.pem;
        include             /etc/letsencrypt/options-ssl-nginx.conf;
        ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

        location / {
            proxy_pass         http://127.0.0.1:8001;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
        }
    }
}
